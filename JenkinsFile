pipeline {
  agent {
    docker {
      image 'node:lts-bullseye-slim'
      args '-p 3000:3000'
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'echo install dependencies'
        sh 'echo > npm install'
      }
      steps {
        sh 'npm install'
      }
    }
  }
}
